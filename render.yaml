services:
  - type: web
    name: zaire-backend
    env: node
    buildCommand: "npm install && pip install -r requirements.txt --quiet || pip3 install -r requirements.txt --quiet || true"
    startCommand: "node index.js"
    envVars:
      - key: RUN_DAEMONS
        value: "true"
